"use client";import {useState} from "react";import {Check,ChevronLeft,Copy,KeyRound,Mail,X} from "lucide-react";
type Row={id:number,name:string,email:string,status:string,createdAt:string,approvedAt:string|null,accessKeyHash:string|null};
export default function AdminRequests({initial}:{initial:Row[]}){const [rows,setRows]=useState(initial),[key,setKey]=useState<{email:string,key:string}|null>(null),[busy,setBusy]=useState<number|null>(null);async function action(id:number,type:"approve"|"reject"){setBusy(id);const r=await fetch("/api/admin/access",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({id,type})});const j=await r.json();setBusy(null);if(r.ok){setRows(p=>p.map(x=>x.id===id?{...x,status:type==="approve"?"approved":"rejected"}:x));if(j.key)setKey({email:j.email,key:j.key})}}return <main className="admin-page"><header className="admin-head"><div><a href="/"><ChevronLeft/></a><span><KeyRound/></span><div><small>ADMINISTRAÇÃO</small><h1>Solicitações de acesso</h1></div></div><b>{rows.filter(r=>r.status==="pending").length} pendentes</b></header>{key&&<section className="key-result"><div><Check/><span><b>Chave criada para {key.email}</b><code>{key.key}</code></span></div><button onClick={()=>navigator.clipboard.writeText(key.key)}><Copy/>Copiar chave</button></section>}<section className="requests-list">{rows.length===0?<div className="empty-requests"><Mail/><h2>Nenhuma solicitação</h2><p>Os novos pedidos aparecerão aqui.</p></div>:rows.map(r=><article key={r.id}><div className="request-avatar">{r.name.slice(0,2).toUpperCase()}</div><div className="request-info"><h3>{r.name}</h3><p>{r.email}</p><small>{new Date(r.createdAt).toLocaleDateString("pt-BR")}</small></div><span className={`request-status ${r.status}`}>{r.status==="pending"?"Pendente":r.status==="approved"?"Aprovado":"Recusado"}</span>{r.status==="pending"&&<div className="request-actions"><button disabled={busy===r.id} onClick={()=>action(r.id,"reject")}><X/>Recusar</button><button className="approve" disabled={busy===r.id} onClick={()=>action(r.id,"approve")}><Check/>Aprovar e gerar chave</button></div>}</article>)}</section></main>}
